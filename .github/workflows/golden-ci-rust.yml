name: Golden CI Rust

on:
  workflow_call:
    
  pull_request:
    branches:
      - master
      - main
    types:
      - opened
      - synchronize
      - edited

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: my-lint-step
        run: echo "Hello World!"
  sast:
    runs-on: ubuntu-latest
    steps:
      - name: my-sast-step
        run: echo "Hello World!"
